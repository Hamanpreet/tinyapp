<!--  Separating the header logic, keep DRY-->
<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
    <span class="navbar-toggler-icon"></span>
    </button>
    <!-- User Login Logic-->
    
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <div style="text-align: right;">
        <% if(user) { %>
          <p>Logged in as: <%= user.email%>
            <form action="/logout" method="POST">
            <button type="submit" class="btn btn-primary">Logout</button></p>
          </form>
          <% } else { %> 
            <a href = "/login" style="margin-right: 10px;">Login</a>
            <a href = "/register" style="margin-right: 10px;">Register</a>
          <% } %> 
          </div>
      </div>
    </div>
  </nav>
</header>